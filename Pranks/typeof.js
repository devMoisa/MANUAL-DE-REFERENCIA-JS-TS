/**
 *  All return of javascript method "typeof" return an the type into a string.
 *
 * Look:
 */

const valueStr = "Hello World";

// ------------------ Here \/ All others types will be "number" or "array" or "object" adnd etc...
if (typeof valueStr === "string") {
  console.log("IS STRING!!");
}
